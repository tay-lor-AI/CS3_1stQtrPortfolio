function square(num){
    return num * num;
}

console.log("Quick Exersise Square: " + square(7))

const greet = function(name) {
    return `Hello, ${name}!`;
  };

  setTimeout(function() {
    console.log("Time's up!");
  }, 1000);

  const greete = (name) => {
    return `Hello, ${name}!`;
  };
  